services:

  # React Frontend (Static Site)
  - type: web
    name: guardian-watch-frontend
    runtime: static
    buildCommand: cd crime-analytics && npm install && npm run build
    staticPublishPath: crime-analytics/dist
